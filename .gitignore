__pycache__/
*.pyc
.idea/

# per-user build outputs - regenerated by ./port.py
/source/*.xml
/keybindings.generated.json
/report.md

# ...but ship one sample keymap + its report so the tool can be tried without an IDE
!/source/default.xml
